import { productCountdown } from 'store/helpers/product-countdown';
import { module, test } from 'qunit';

module('Unit | Helper | product countdown');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = productCountdown([42]);
  assert.ok(result);
});
