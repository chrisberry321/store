import { dateHelper } from 'store/helpers/date-helper';
import { module, test } from 'qunit';

module('Unit | Helper | date helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateHelper([42]);
  assert.ok(result);
});
